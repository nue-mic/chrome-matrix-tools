# Chrome 多开快捷方式生成器

一个零安装、双击即用的图形化工具，批量生成“Chrome 多账号分身”的快捷方式。
是 `ChromeGenerator.ps1` 的 GUI 复刻版，**生成的快捷方式与旧脚本完全兼容**。

> **关于 `Chrome多开生成器.exe`（独立版）**：已把 .NET 运行时一起打包进去，**任何 64 位 Windows 双击即用，无需安装 .NET 或任何东西**。文件约 60MB 属正常（自包含的固有大小）。发给客户的就是这一个 exe。

## 怎么用（一句话）

双击 **`Chrome多开生成器.exe`** → 确认路径/前缀/数量 → 点【一键生成】。
生成完成后会自动在桌面放一个「Github集合」入口（可改名），双击直达快捷方式文件夹。

## 界面说明

| 项 | 说明 |
|---|---|
| **基本目录** | 所有路径的根，默认 `D:\Chrome_Matrix_Browser`。改它，下面所有路径自动跟随。 |
| **名称** | 项目名，默认 `Github`。改它，下面路径里的 `<名称>_Multiple` / `<名称>_xxx` / 前缀全部自动跟随（仍可手动改）。 |
| **Chrome 路径** | 启动时自动探测（注册表 + 常见安装目录）。探不到就点【浏览…】手选 `chrome.exe`。 |
| **数据目录** | 每个分身的独立环境（Cookie/登录态/插件）存放处。默认 `<基本目录>\<名称>_Multiple\<名称>_UserData`。 |
| **快捷方式目录** | 生成的 `.lnk` 输出位置。默认 `<基本目录>\<名称>_Multiple\<名称>_ShortCuts`。 |
| **名称前缀** | 分身命名前缀，默认 `<名称>_`（如 `Github_`）。跟随“名称”，也可手动改。 |
| **起始 / 结束** | 生成的编号范围，如 1~20 生成 `前缀1`…`前缀20`（上限 10000）。 |
| **已存在时** | 单选：**跳过**（默认，已有的分身/`.lnk` 不动）/ **覆盖**（强制重建，模板模式下会删除已有分身数据）。 |
| **使用模板** | 勾选后，把“模板目录”里配好的 UserData **整套复制**到每个分身，让分身一出生就是已登录/已装扩展状态。不勾＝原逻辑（只建空快捷方式）。 |
| **模板目录** | 你手工配好的样板 UserData 文件夹。默认 `<基本目录>\<名称>_Multiple\<名称>_Template_UserData`，**不管是否勾选“使用模板”，它都跟随 基本目录/名称 变动**。 |
| **排除缓存目录** | 默认勾选，复制时跳过 `Cache/GPUCache` 等可再生文件，**更快更省空间**；登录态/扩展/设置照常保留。 |
| **桌面入口名称** | 桌面快捷方式的名字，默认 `Github集合`。 |
| **生成后在桌面创建该入口** | 默认勾选。生成完成后在**桌面**创建一个该名字的快捷方式，双击直达快捷方式文件夹；已存在则**直接覆盖**，不会重复堆叠。 |
| **一键生成** | 开始批量生成，下方有日志和进度条。 |
| **打开输出文件夹** | 生成后直接打开 `.lnk` 所在文件夹，双击即可开浏览器分身。 |
| **建桌面入口** | 不重新生成，单独在桌面创建/覆盖那个入口快捷方式。 |

窗口支持**拖拽边缘自由缩放**，卡片与输入框会跟随宽度自适应排列。

## 模板功能怎么用

1. 先用某个分身把 Chrome 配置好（登录账号、装扩展、设代理/书签…）。
2. **完全关闭**该 Chrome（否则 Cookies 等文件被锁，登录态拷不过去——程序会提示）。
3. 把那个 UserData 文件夹复制成模板，放到 exe 同目录改名 `Template_UserData`，或在界面“模板目录”里【浏览…】指定。
4. 勾选 **使用模板** → 设好前缀/数量 → 【一键生成】。

> ⚠️ **安全说明**
> - 模板复制遵循上方“已存在时”单选：**跳过**＝只给新分身复制、保护你已登录的 Github_1/2/21… 不动；**覆盖**＝先删除已有分身数据再用模板重写（会丢已有登录，程序会**二次弹窗确认**，并拒绝把系统/真实 Chrome 目录当数据目录）。
> - **跳过模式是“叠加复制”**：若目标分身已有内容会整体跳过、不会清理残留；想干净重建请用**覆盖**。
> - 若复制时有文件**被占用**（Chrome 没关干净），这些文件会被跳过，完成弹窗会变成 ⚠ **警告**并提示“N 个分身 M 个文件未复制、可能缺登录态”——看到就关掉 Chrome 用“覆盖”重跑那几个。
> - 数据目录不能是盘符根（如 `D:\`）；模板目录不能是数据目录本身或其父级（程序均会拦截，避免误删/无限递归）。
> - 同机同 Windows 账户下克隆，登录态（DPAPI 加密的 Cookie）可正常迁移。

## 原理

每个 `.lnk` 本质是：
```
chrome.exe --user-data-dir="<数据目录>\<前缀><编号>"
```
（路径加引号，含空格的目录也不会被截断；与旧脚本生成的无引号快捷方式指向同一份分身数据。）
`--user-data-dir` 让每个分身拥有**完全独立**的登录态，互不串号——这就是 Chrome 多账号管理的核心机制。

## 关于配置

**不使用任何配置文件**。所有默认值都写死在 exe 里，每次启动都是干净的默认值（基本目录 `D:\Chrome_Matrix_Browser`、名称 `Github` 等）。想换项目，改“名称”即可一键联动。

## 想改源码重新打包

1. 编辑 `ChromeShortcutGenerator.cs`
2. 双击 **`打包独立版.bat`**（需本机装有 .NET SDK；会自动打包成自包含单文件）
3. 得到新的 `Chrome多开生成器.exe`（独立版，覆盖旧的）

> **换图标**：图标由 `MakeIcon.cs` 在打包时绘制成 `app.ico` 并嵌入 exe。想换：改 `MakeIcon.cs` 的绘制逻辑，删掉旧的 `app.ico`，再双击 `打包独立版.bat` 即可重新生成。

> ⚠️ `打包独立版.bat` 是 **GBK 编码 + CRLF 换行**（中文批处理双击运行的必须条件）。**不要用记事本“另存为 UTF-8”**，否则双击会乱码失效；要改内容请用支持 GBK/ANSI 的编辑器，或直接找作者改。

## 交付清单

| 文件 | 作用 |
|---|---|
| `Chrome多开生成器.exe` | **主程序（独立版）**，任何 64 位 Windows 双击即用，**无需安装 .NET**。发给客户的就是它 |
| `打包独立版.bat` | 一键重新打包独立版（需本机 .NET SDK） |
| `ChromeShortcutGenerator.cs` | C# 源码 |
| `MakeIcon.cs` | 应用图标生成器源码（打包时自动绘制 `app.ico`） |
| `app.ico` | 应用图标（多尺寸高清·蓝紫渐变·层叠浏览器窗口） |
| `pack/pack.csproj` | 打包配置（自包含单文件发布） |
